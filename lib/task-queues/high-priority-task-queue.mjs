import {
    Task,
    TaskQueue
} from "../registry.mjs";

export class HighPriorityTaskQueue extends TaskQueue {
    /**
     * @param { Task } task
     */
    constructor() {
        super(task);
    }
}